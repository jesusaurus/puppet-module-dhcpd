		pool {
<% scope.lookupvar('deny').each do |item| -%>
			deny <%= item %>;
<% end -%>
<% scope.lookupvar('allow').each do |item| -%>
			allow <%= item %>;
<% end -%>
			range <%= scope.lookupvar('range') %>;
			default-lease-time <%= scope.lookupvar('default_lease') %>;
			max-lease-time <%= scope.lookupvar('max_lease') %>;
		}
